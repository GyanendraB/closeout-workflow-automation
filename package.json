{
  "name": "closeout-task-solution",
  "author": "Gyan",
  "private": true,
  "scripts": {
    "test": "npm --prefix closeout-ai-validation-automation test",
    "test:ui": "npm --prefix closeout-ai-validation-automation run test:ui",
    "test:api": "npm --prefix closeout-ai-validation-automation run test:api",
    "report": "npm --prefix closeout-ai-validation-automation run report",
    "report:allure": "npm --prefix closeout-ai-validation-automation run report:allure",
    "report:allure:generate": "npm --prefix closeout-ai-validation-automation run report:allure:generate"
  }
}
