export const DEFAULT_TIMEOUT = 50000; // Standard timeout shared by helpers when no local override is needed.
export const REPORT_DIR = "reports"; // Default folder name for generated reports.
export const CONTROL_PANEL_TEST_PROJECT_NUMBER = 753; // Project identifier used by control panel UI tests.
export const CONTROL_PANEL_TEST_SITE_NUMBER = 6; // Site identifier used by control panel UI tests.
